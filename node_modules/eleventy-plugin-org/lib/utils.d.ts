import { Plugin } from "unified";
import { Node } from "unist";
declare module "unified" {
    interface CompileResultMap {
        Node: Node;
    }
}
declare module "vfile" {
    interface DataMap {
        links: Set<string>;
        backlinks: Map<string, {
            slug: string;
            title: string;
        }>;
        images: {
            to: string;
            from: string;
        }[];
        slug: string;
        title: string;
        date: string;
        tags: string[];
        ids: Record<string, string>;
    }
}
/** A primitive compiler to return node as is without stringifying. */
export declare const fromJson: Plugin;
/** A primitive compiler to return node as is without stringifying. */
export declare const toJson: Plugin;
export declare const parseTimestamp: (r: string) => string;
